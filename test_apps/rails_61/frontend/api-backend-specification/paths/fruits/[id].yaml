---
get:
  parameters:
  - in: path
    name: id
    description: id
    schema:
      type: integer
    required: true
    example: '1'
  responses:
    "'200'":
      description: GET /fruits/:id -> 200
      content:
        application/json:
          schema:
            "$ref": "../../components//Fruit.yaml"
